FROM node:latest
#베이스 이미지를 정의, node.js 이미지에 태그는 최신버전인 이미지를 베이스 이미지로 사용 *이미지 태그는 해당 이미지의 버전이나 레이블을 나타내는 식별자
COPY ./ ./
#현재 디렉토리(./)의 모든 파일과 폴더를 도커 이미지의 / 경로로 복사 *애플리케이션 코드를 도커 이미지 안으로 복사하는 역할
RUN npm install
#도커 이미지 안에서 npm install 명령어를 실행하여 Node.js 애플리케이션에 필요한 패키지를 설치
CMD ["node", "index.js"]
# 도커 컨테이너가 시작될 때 index.js 파일을 실행